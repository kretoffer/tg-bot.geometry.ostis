[project]
name = "ostis-geometry-tgbot"
requires-python = ">=3.9"

[tool.ruff]
line-length = 100
target-version = "py39"
fix = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
minversion = "7.0"
addopts = "-ra -q"
testpaths = [
    "tests"
]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning"
]
pythonpath = [".", "src"]
